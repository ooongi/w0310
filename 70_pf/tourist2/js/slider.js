$(document).ready(function () {
    $(".bxslider").bxSlider({
        mode: "fade",
        auto: true,
        // play, pause
        autoControls: false,
        stopAutoOnClick: true,
        // pagenation(indicator) 하단 중앙의 동그라미 버튼
        pager: true,
        slideWidth: 1905,
    });
});
